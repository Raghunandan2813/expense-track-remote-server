[project]
name = "expense-track-remote-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.2.4",
    "supabase==2.25.1",
    "python-dotenv",
    "matplotlib",
    "httpx",
]
